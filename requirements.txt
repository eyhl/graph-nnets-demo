# Runtime Python dependencies (pip-installable).
# Note: torch / torch-geometric are installed via conda for this project (see environment.yml).

click==8.0.3
hydra-core
matplotlib==3.8.4
numpy==1.26.4
omegaconf~=2.1.1
python-dotenv==0.19.2
wandb  # optional (enabled via config)

# Install this repo as a package
-e .
