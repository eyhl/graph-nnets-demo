Getting started
===============

Local setup:

- Create a virtualenv and install dependencies.
- Run training and evaluation from `src/models/`.

Data is downloaded automatically via PyTorch-Geometric (Cora).